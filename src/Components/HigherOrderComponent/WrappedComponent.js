import React from "react";

function WrappedComponent() {
    return(
        <div>
            Hello All - I am displaying from inside the wrapped component passed to Higher order component !
        </div>
    )
}

export default WrappedComponent;